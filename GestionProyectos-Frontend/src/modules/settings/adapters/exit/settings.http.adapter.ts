/**
 * Exit adapter: implementación del puerto settings.repository
 * usando HTTP (placeholder). Cuando exista backend, llenar aquí.
 */
export const settingsHttpAdapter = {
  get: async () => {
    return null;
  },
  save: async (_prefs) => {
    return null;
  }
};
